import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    checkBoxBlock: {
        flexDirection: "row",
        alignItems: "center",
        marginBottom: 20,

    },
    text: {
        marginLeft: 10,
        color: "#323C4C",
    }
});